
function poziomGlosnosci () {
    
};

function glosnoscNaPlus (dane = poziomGlosnosci) {
    dane += 1;
return dane
};

function glosnoscNaMinus(dane = poziomGlosnosci){
    dane -= 1
return dane
};